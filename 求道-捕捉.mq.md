---
title: 求道-捕捉
description: 不经 LLM，直接把一条笔记写成 data/runs/*.mq.md 并入库。
import run:lib/run.mq.md
import db:db/index.mq.md
import sys:lib/sys.mq.md
---

# main

*title = > sys.env_get name="QDAGENT_TITLE"*
1. not `title`
  *title = "手记捕捉"*

*task = > sys.env_get name="QDAGENT_TASK"*
1. not `task`
  *task = "用 Marqdo 写下一条可审计笔记。"*

*result = > sys.env_get name="QDAGENT_RESULT"*
1. not `result`
  *result = "已沉淀到 data/runs/，可在笔记库与 marqdo view 中打开。"*

*slug = > sys.env_get name="QDAGENT_SLUG"*
1. not `slug`
  *slug = ""*

*store = > db.open*
*path = > run.沉淀 store=`store` title=`title` task=`task` result=`result` slug=`slug`*
> print text=`path`
**path**
