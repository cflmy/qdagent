---
title: 求道-列出
description: 列出最近笔记（runs 表），供 MCP / 桥接脚本使用。
import db:db/index.mq.md
import sys:lib/sys.mq.md
import json:lib/json.mq.md
---

# main

*lim_s = > sys.env_get name="QDAGENT_LIMIT"*
1. not `lim_s`
  *lim_s = "20"*
*limit = > int value=`lim_s`*

*rows = > db.recent limit=`limit`*
*payload = > json.stringify value=`rows`*
*wrapped = "{\"ok\":true,\"runs\":" + `payload` + "}"*
> print text=`wrapped`
**wrapped**
