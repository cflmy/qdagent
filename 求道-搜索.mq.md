---
title: 求道-搜索
description: 关键词检索 data/runs（agent.corpus_search）。
import api:lib/api.mq.md
import sys:lib/sys.mq.md
import json:lib/json.mq.md
---

# main

*query = > sys.env_get name="QDAGENT_QUERY"*
1. not `query`
  *query = ""*
*top_s = > sys.env_get name="QDAGENT_TOP_K"*
1. not `top_s`
  *top_s = "5"*
*top_k = > int value=`top_s`*
*out = > api.search query=`query` top_k=`top_k`*
*raw = > json.stringify value=`out`*
> print text=`raw`
**raw**
