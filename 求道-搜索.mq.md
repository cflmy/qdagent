---
title: 求道-搜索
description: 导出最近笔记窗口供关键词过滤（GAP-05）。
import db:db/index.mq.md
import sys:lib/sys.mq.md
import json:lib/json.mq.md
---

# main

*rows = > db.recent limit=200*
*payload = > json.stringify value=`rows`*
*wrapped = "{\"ok\":true,\"mode\":\"keyword\",\"runs\":" + `payload` + "}"*
> print text=`wrapped`
**wrapped**
