---
title: 求道-整理
description: 手动整理笔记库 — 写出 data/kb/整理-*.mq.md 与审计 run（不改写历史 runs）。
import api:lib/api.mq.md
import json:lib/json.mq.md
import sys:lib/sys.mq.md
---

# main

*query = > sys.env_get name="QDAGENT_QUERY"*
1. not `query`
  *query = "求道"*
*limit_s = > sys.env_get name="QDAGENT_LIMIT"*
1. not `limit_s`
  *limit_s = "12"*
*limit = > int value=`limit_s`*
*out = > api.organize query=`query` limit=`limit`*
*raw = > json.stringify value=`out`*
> print text=`raw`
**raw**
