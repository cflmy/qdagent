---
title: 求道-询问
description: 带用户画像与笔记检索的 LLM 询问；结果沉淀为 .mq.md + 笔记库，并轻量更新画像。
import llm:ext/ai/llm.mq.md
import agent:ext/ai/agent.mq.md
import json:lib/json.mq.md
import run:lib/run.mq.md
import db:db/index.mq.md
import api:lib/api.mq.md
import sys:lib/sys.mq.md
import time:lib/time.mq.md
---

## 今日

*u = > time.now_unix*
**> time.format unix=`u` pattern="%Y-%m-%d"**

## 联网搜索
    + `query`=""

1. not `query`
  *query = > sys.env_get name="QDAGENT_TASK"*
*out = > api.web_search query=`query` limit=5*
**out**

# main

> llm.load_env path=".env"

*task = > sys.env_get name="QDAGENT_TASK"*
1. not `task`
  *task = "用一句话说明求道智能体与普通聊天机器人的差别。"*

*key = > sys.env_get name="OPENAI_API_KEY"*
1. not `key`
  *key = > sys.env_get name="MARQDO_LLM_API_KEY"*

*ctx = > api.context query=`task` top_k=5*
*profile = ctx[^profile]*
*hits = ctx[^hits]*
*hits_s = > json.stringify value=`hits`*
*web = > api.web_search query=`task` limit=5*
*web_s = > json.stringify value=`web`*
*standing = "你是求道助手。回答简洁；优先引用已有笔记证据（evidence only）。联网结果仅供参考需交叉验证。系统会自动沉淀新 runs（只追加）。清理/改写画像时先说明结论，并附 ```qd-change``` 完整新正文块，由用户在界面点「同意应用」后写入；不要声称已改磁盘。需要日期时 CALL:今日。需要实时信息时可 CALL:联网搜索。\n\n# 用户画像\n\n" + `profile` + "\n\n# 笔记证据\n\n" + `hits_s` + "\n\n# 联网证据\n\n" + `web_s`*
*enriched = "# 上下文\n\n" + `hits_s` + "\n\n# 联网\n\n" + `web_s` + "\n\n# 任务\n\n" + `task`*

*result = ""*
*mode = "dry"*

1. `key`
  `工具表` =

  | 工具 |
  |------|
  | 今日 |
  | 联网搜索 |

  *model = > llm.llm*
  *助手 = > agent.agent model=`model` tools=`工具表` standing=`standing`*
  *out = > `助手`.step task=`enriched` writeback=True*
  *status = > json.get value=`out` key="status"*
  1. `status` == "ok"
    *result = > json.get value=`out` key="result"*
    *mode = "live"*
  2. *
    *err = > json.get value=`out` key="error"*
    *result = "询问失败：" + `err`*
    *mode = "error"*
2. *
  *result = "（无 OPENAI_API_KEY，干跑沉淀）求道把每次执行写成 .mq.md 审计笔记，并检索画像与笔记库。任务：" + `task` + "\n证据：" + `hits_s` + "\n联网：" + `web_s`*
  *mode = "dry"*

*store = > db.open*
*title = "询问 · " + `mode`*
*path = > run.沉淀 store=`store` title=`title` task=`task` result=`result`*
> api.profile_update task=`task` result=`result`
> print text=`mode`
> print text=`path`
> print text=`result`
**path**
